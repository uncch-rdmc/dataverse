pipeline {
  agent any

  stages {

    stage('Build') {
      steps {
	sh 'cd doc/sphinx-guides/'
	sh 'make clean'
	sh 'make html'
	sh 'make epub'
      }
    }

    stage('Test') {
      steps {
	sh 'cd doc/sphinx-guides/'
	sh 'make doctest'
      }
    }

    stage('Post') {
      steps {
	sh 'cd doc/sphinx-guides/'
	sh 'cp build/epub/Dataverse.epub build/html'
	sh 'zip -r ${WORKSPACE}/${CHANGE_BRANCH}.zip ./'
      }
    }
  }
}
